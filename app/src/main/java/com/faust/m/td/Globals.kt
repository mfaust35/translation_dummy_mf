package com.faust.m.td

// I don't know if this would be the right place to put all global variable
// It does not seem worse than any other random place
const val DATABASE_PATH = "translation_database"

const val DEFAULT_USER_ID = 1.toLong()